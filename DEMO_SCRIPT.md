# Demo Script — Privacy AI Helper

> 录屏时长建议：3 分钟以内
>
> 重点展示：地址输入 → 侧边栏授权 → 越权拦截 → 快速授权重试 → 合约验证

---

## 场景设定

**用户角色：** 一位关注健康数据的普通人

**场景：** 想知道自己的血压是否正常，但不希望血压数据泄露给任何第三方

---

## 演示步骤

### Step 1: 打开应用 (0:00 - 0:12)

- 双击 `一键启动.bat`（不录这段，直接展示已运行的界面）
- 展示深色毛玻璃 UI
- 指出顶部地址输入框和"确认"按钮
- 指出提示条 "⚠ 请输入你的 Aleo 地址以开始对话"

### Step 2: 输入 Aleo 地址连接 (0:12 - 0:30)

- 在顶部地址输入框粘贴 Aleo 地址（如 `aleo1xxx...xxxx`）
- 点击"确认"或回车
- **侧边栏自动展开：** 4 个类别卡片全部显示 ❌ 未授权，"0/4 已授权"
- 聊天区提示："请先在左侧授权一个数据类别"
- 旁白："连接后，所有数据类别默认未授权。用户必须显式授权，AI 才允许在该类别下回答。"

### Step 3: 侧边栏授权管理 + 收起展示 (0:30 - 0:55)

**侧边栏特写：**
- 4 个类别卡片：❤健康 ❌ / 🧬基因 ❌ / 💰财务 ❌ / 📋其他 ❌
- 顶部显示 "0/4 已授权"
- 底部显示 "Aleo zkVM · Poseidon2 · SNARK"
- 旁白："每个授权对应链上的 Poseidon2 ZK 哈希，Aleo zkVM 生成 SNARK 证明。前端实时展示 ZK 标签和哈希值。"

**展示收起/展开功能：**
- 点击 « 按钮 → 侧边栏收起为窄条，只保留 » 展开按钮
- 聊天区自动扩宽，不挡视线
- 点击 » → 侧边栏恢复
- 旁白："授权面板可随时收起，不影响聊天体验。"

**授权健康数据：**
- 点击 ❤健康 卡片的"授权 24h"按钮
- 卡片立即变为 ✅ 已授权，显示 ⏳23h59m 倒计时和 ZK 哈希
- 顶部变为 "1/4 已授权"
- 聊天区提示变为 "你好，请输入你的问题"
- 旁白："点击授权后，前端即时显示 Poseidon2 ZK 哈希和过期倒计时。24 小时后自动失效。"

---

### Step 4: 三轮对话 — 示范完整数据边界 (0:55 - 2:15)

#### 第 1 轮：健康已授权 → 正常回答 (0:55 - 1:10)

- 输入框下拉选择 "❤ 健康"（或直接点击侧边栏健康卡片）
- 输入：**"我最近测量的血压是 135/85，正常吗？"**
- 点击发送
- AI 中文回答 + 底部 Privacy Tag
- **镜头特写 Privacy Tag + "合约验证 →" 链接**
- 旁白："每一条 AI 回答附带独立的 Privacy Tag，绑定授权类别 + AI 输出 + 时间戳。第一轮通过。"

#### 第 2 轮：财务未授权 → 越权拦截 + 快速授权重试 (1:10 - 1:45)

- 下拉切换到 "💰 财务"
- 输入：**"我的银行卡余额是多少？"**
- 点击发送
- **AI 拒绝回答：** "您尚未授权财务数据类别的访问……"
- 拒绝消息下方显示 **"🔓 授权并重试"** 按钮
- **点击"授权并重试"按钮 → 演示完整链路：**
  - 按钮变灰"已授权"
  - 侧边栏财务卡片立即变为 ✅ 已授权，"2/4 已授权"
  - 拒绝消息保留不动
  - AI 自动重新回答（基于新授权）
  - 新回答 + 新 Privacy Tag 追加到聊天区
- 旁白："拒绝不是死胡同。点击授权并重试，一键完成授权+重新回答。拒绝消息保留作为审计轨迹，新回答附带独立的 Privacy Tag。"

#### 第 3 轮：撤销健康 → 验证撤销生效 (1:45 - 2:05)

- 点击侧边栏健康卡片的"撤销"按钮
- 卡片立即变为 ❌ 未授权，"1/4 已授权"
- 下拉切换到 "❤ 健康"
- 输入：**"那我的心率 72 次/分钟正常吗？"**
- 点击发送
- **再次被拦截！** — 因为健康已被撤销
- **对比第 1 轮：** 之前能回答，现在不能——证明撤销即时生效
- 再次点击"授权并重试" → 自动回答心率问题
- 旁白："撤销即时生效，第 1 轮能回答的问题现在被拦截。任意类别可独立授权/撤销，精细控制每一个数据边界。"

#### 三轮总结 (2:05 - 2:15)

| 轮次 | 类别 | 操作 | 结果 |
|------|------|------|------|
| 1 | 健康（已授权） | 血压问题 | ✅ 正常回答 |
| 2 | 财务（未授权） | 余额问题 → 授权并重试 | ❌→✅ 拦截→一次点击完成授权+重答 |
| 3 | 健康（已撤销） | 心率问题 | ❌ 拦截（证明撤销生效） |

最后再次展示侧边栏：健康❌ 财务✅ 基因❌ 其他❌，"1/4 已授权"

---

### Step 5: 展示合约部署证明 (2:15 - 2:35)

- 点击侧边栏 "📦 合约已部署 →" 链接
- 打开 Aleo Testnet 区块浏览器
- 展示真实部署交易: `at1tlrj2xsah...` (v2)
- 说明：v3 已编译 (2.16 KB, 55,593 约束)，因 Testnet 费率偏差暂未重新部署
- 强调：合约只存 Poseidon2 哈希值，不存原始数据
- 一句话：**合约已部署到 Aleo Testnet，任何人都可以在链上验证。**

### Step 6: 展示 ZK 架构总结 (2:35 - 3:00)

镜头回到侧边栏底部，旁白总结：

> "三层隐私防线：第一层，本地 AI 推理，数据从不离开用户电脑。第二层，Aleo zkVM 零知识证明，Poseidon2 哈希链上存证，观察者只能看到 field 值，无法反推类别。第三层，Privacy Tag 可用性承诺，SHA256 绑定授权上下文与 AI 输出，任何人可重算验证。"

---

## 关键台词

| 时刻 | 台词 |
|------|------|
| 连接地址 | "连接后所有类别默认未授权，用户必须显式授权。" |
| 侧边栏特写 | "每个授权对应链上的 Poseidon2 ZK 哈希，Aleo zkVM 生成 SNARK 证明。" |
| 收起侧边栏 | "授权面板可随时收起，不影响聊天体验。" |
| 第 1 轮 AI 正常回答 | "每一条 AI 回答附带独立的 Privacy Tag。第一轮通过。" |
| 第 2 轮 授权并重试 | "拒绝不是死胡同。一键完成授权+重新回答，拒绝消息保留作为审计轨迹。" |
| 第 3 轮 撤销 | "撤销即时生效，第 1 轮能回答的问题现在被拦截。" |
| 展示合约 | "链上只存 Poseidon2 哈希值，即使下载整个区块链也无法反推你授权的是什么。" |
| 总结 ZK 架构 | "三层隐私防线：本地推理不出设备，Aleo zkVM 做链上 ZK 存证，Privacy Tag 做链下可用性承诺。" |

---

## 录屏设置建议

- 分辨率：1920x1080
- 缩放：100%（不要用 125% 或 150%）
- 隐藏浏览器书签栏
- 关闭通知和弹窗
- OBS 或系统自带录屏工具均可

---

## 评委 Q&A 弹药库

| 评委可能的提问 | 你的应答（背下来） |
|--------------|-----------------|
| "你这个 Privacy Tag 不是真 ZK，就是 SHA256 对吧？" | "对，Layer2 链下承诺是 SHA256。但 Layer1 链上用的是 Aleo 原生 Poseidon2，每一个 mapping 操作都在 SNARK 约束内。这是两层证明体系，SHA256 绑定的是上下文，ZK 保护的是链上授权状态。两者分工明确。" |
| "如果 Leo Wallet 插件没装，你的 Demo 怎么办？" | "前端顶部有地址输入框，直接从钱包插件复制 Testnet 地址粘贴即可连接。合约的浏览器验证链接是独立的，不需要钱包也能查链上交易。" |
| "如果 Ollama 没启动，你的 Demo 还能跑吗？" | "有 Plan B。Demo 模式传 `?demo=true` 参数，后端直接返回预设回答 + Privacy Tag，不依赖 Ollama。" |
| "Testnet 万一挂了，交易记录查不到呢？" | "我们保留了完整的部署记录和编译产物，即使 Testnet 不可达，评委也能直接看合约源码。" |
| "AI 和 Privacy 在你的项目里，到底有什么真正的交叉？" | "`is_authorized` 函数是交叉点。它在 AI 被调用前执行 ZK 授权验证。Privacy Tag 在 AI 回答后绑定授权上下文。不是 AI 做隐私，是隐私系统包裹 AI。" |
| "为什么用本地 AI，不用云端模型？" | "隐私的核心是数据不要离开用户设备。GPT-4 再强，你把血压数据传给 OpenAI 的那个瞬间，隐私就失效了。本地 Ollama + Qwen2.5 1.5B 保证了数据零离开。" |
| "和 Aztec / Tornado 比，为什么选 Aleo？" | "Aztec 主要做隐私交易，Tornado 做混币。Aleo 是唯一原生支持 Poseidon2 + Leo 合约语言 + ZK storage mapping 的 Layer1。我们的项目是做 ZK 授权门禁，`mapping(field => field)` 是这场景的最优选择。" |
