# 测试操作流程 — Privacy AI Helper

> 在提交前逐项验证所有功能是否正常。

---

## 前置条件

| 组件 | 要求 | 检查命令 |
|------|------|---------|
| Node.js | >= 18 | `node --version` |
| Ollama | 已安装 + 服务运行中 | `curl http://localhost:11434/api/tags` |
| qwen2.5:1.5b | 已下载 | 同上，列表中应有 `qwen2.5:1.5b` |
| Leo Wallet | 浏览器插件已安装 | 打开任意页面，F12 → 控制台输入 `window.aleo` 应返回对象 |
| 端口 | 3001 / 5173 未被占用 | `netstat -ano | findstr 3001` |

---

## 第一步：启动服务

### 方式 A：一键启动（推荐）

双击 `一键启动.bat`，等待 3 个窗口弹出：
- 窗口 1：Ollama 服务
- 窗口 2：后端 Express（3001）
- 窗口 3：前端 Vite（5173）

浏览器会自动打开 `http://localhost:5173`。

### 方式 B：手动启动

```bash
# 终端 1
ollama serve

# 终端 2
cd backend
npm install
node index.js
# 看到 "Backend on http://localhost:3001" 即为成功

# 终端 3
cd frontend
npm install
npm run dev
# 看到 "Local: http://localhost:5173/" 即为成功
```

---

## 第二步：功能测试（10 项）

### 测试 1：后端健康检查

| 操作 | 预期 |
|------|------|
| 浏览器打开 `http://localhost:3001/api/health` | 返回 `{"status":"ok","ollama":"http://localhost:11434"}` |

### 测试 2：后端 Demo 降级模式

| 操作 | 预期 |
|------|------|
| 浏览器打开 `http://localhost:3001/api/demo?lang=zh&cat=1` | 返回中文健康回答 + `privacy_tag` + `source: "demo"` |

### 测试 3：前端页面加载

| 操作 | 预期 |
|------|------|
| 浏览器打开 `http://localhost:5173` | 深色极简 UI 正常显示，顶部有"连接钱包"按钮，底部有输入框 |

### 测试 4：钱包连接（Plan A：真实钱包）

| 操作 | 预期 |
|------|------|
| 点击"连接钱包" | Leo Wallet 插件弹出授权窗口 |
| 点击"Connect"确认 | 显示绿色徽章 + 钱包地址 |

### 测试 5：钱包连接（Plan B：演示降级）

| 操作 | 预期 |
|------|------|
| 不装钱包插件，点击"连接钱包" | 3 次重试后自动显示演示模式（地址 `aleo1demo...`） |

### 测试 6：语言切换

| 操作 | 预期 |
|------|------|
| 选择 "中文" | 输入框占位符显示"请输入您的问题..." |
| 选择 "English" | 占位符切换为英文 |
| 用中文模式发送问题 | AI 用中文回答 |

### 测试 7：类别选择器

| 操作 | 预期 |
|------|------|
| 下拉框切换"健康数据/基因数据/财务数据/其他数据" | 选项正常切换，选中值随之更新 |

### 测试 8：三轮对话（核心测试）

**前置：** 刷新页面 → 连接钱包（或进入演示模式）

| 轮次 | 类别 | 输入 | 预期结果 |
|------|------|------|---------|
| 1 | 健康数据 (1) | "血压135/85正常吗？" | ✅ `verified: true`，中文正常回答，底部显示 Privacy Tag + "合约验证 →" |
| 2 | 财务数据 (3) | "我的银行卡余额是多少？" | ❌ `verified: false`，`source: "authorization_blocked"`，提示"尚未授权财务数据类别" |
| 3 | 健康数据 (1) | "心率72次/分钟正常吗？" | ✅ `verified: true`，中文正常回答，Privacy Tag **不同于** 第 1 轮 |

### 测试 9：Privacy Tag 唯一性

| 操作 | 预期 |
|------|------|
| 对比测试 8 中第 1 轮和第 3 轮的 Privacy Tag | **两个 Tag 完全不同**（因为绑定了不同的 AI 回答内容） |

### 测试 10：合约浏览器链接

| 操作 | 预期 |
|------|------|
| 连接钱包后，点击状态栏的"链上验证 →" | 跳转到 Provable Explorer，显示 tx `at1tlrj2xsah...` |
| 点击任意 AI 回答下的"合约验证 →" | 同上，跳转到区块浏览器 |

---

## 第三步：异常场景测试（3 项）

### 测试 11：Ollama 未启动时的降级

| 操作 | 预期 |
|------|------|
| 关闭 Ollama → 发送问题 | AI 回答前缀 `[AI Fallback]`，不崩溃 |
| 改用 `?demo=true` 参数 | 返回预设 Demo 回答，"血压135/85属于正常高值..." |

### 测试 12：刷新页面后状态保持

| 操作 | 预期 |
|------|------|
| 连接钱包 → 发一条消息 → F5 刷新 | 钱包状态重置（需重新连接），聊天记录清空（当前设计，非 bug） |

### 测试 13：网络断开时的表现

| 操作 | 预期 |
|------|------|
| 断开网络 → 发送问题 | Ollama 本地推理不受影响，正常返回回答 |

---

## 测试结果记录

| # | 测试项 | 结果 | 备注 |
|---|--------|------|------|
| 1 | 后端健康检查 | [ ] | |
| 2 | Demo 降级模式 | [ ] | |
| 3 | 前端页面加载 | [ ] | |
| 4 | 钱包连接 Plan A | [ ] | |
| 5 | 钱包连接 Plan B | [ ] | |
| 6 | 语言切换 | [ ] | |
| 7 | 类别选择器 | [ ] | |
| 8 | 三轮对话 | [ ] | |
| 9 | Privacy Tag 唯一性 | [ ] | |
| 10 | 合约浏览器链接 | [ ] | |
| 11 | Ollama 降级 | [ ] | |
| 12 | 刷新保持 | [ ] | |
| 13 | 断网表现 | [ ] | |

---

## 快速排错

| 症状 | 检查 |
|------|------|
| 前端 5173 打不开 | 确认 `frontend` 目录下 `npm run dev` 已启动 |
| 后端 3001 无响应 | 检查终端是否有报错，端口是否被占用 |
| AI 回复 `[AI Fallback]` | Ollama 未启动 → 运行 `ollama serve` |
| 钱包连接报错 `window.aleo undefined` | Leo Wallet 插件未安装或未解锁 |
| 财务类别未拦截 | 刷新页面重新连接钱包（授权状态是会话级的） |
| 中文回答变英文 | 检查语言下拉框是否选了"中文" |

---

> 所有 13 项测试通过后，即可开始录制 Demo 视频。
